#!/usr/bin/env bash

dateoptions="$@"

__journal_dir="${HOME}/journals"
mkdir -p -v ${__journal_dir}
__journal_file="${__journal_dir}/$(date -d "$dateoptions" +%F).md"

pushd ${__journal_dir} > /dev/null
"${EDITOR:-vi}" "${__journal_file}"
popd > /dev/null
