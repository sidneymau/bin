#!/usr/bin/env bash

__journal_dir="${HOME}/journals"
mkdir -p -v ${__journal_dir}
__journal_file="${__journal_dir}/$(date +%F).md"

# printf -- '- %s: \n' "$(date '+%r')" >> ${__journal_file}

"${EDITOR:-vi}" "${__journal_file}"
